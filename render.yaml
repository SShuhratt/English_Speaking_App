services:
  # Web service for Laravel Octane (frankenphp)
  - type: web
    name: edtech-api
    env: docker
    dockerfilePath: Dockerfile
    plan: free
    envVars:
      - key: PORT
        value: 8000
      - key: APP_ENV
        value: production
      - key: APP_DEBUG
        value: false
      - key: APP_KEY
        generateValue: true
      - key: DB_CONNECTION
        value: pgsql
      - key: DB_URL
        fromDatabase:
          name: edtech-db
          property: connectionString
      - key: REDIS_CLIENT
        value: phpredis
      - key: REDIS_URL
        sync: false # Set this to your Upstash Redis connection string (e.g. rediss://...)
      - key: CACHE_STORE
        value: redis
      - key: QUEUE_CONNECTION
        value: redis
      - key: SESSION_DRIVER
        value: redis

  # Background worker for Horizon queue
  - type: worker
    name: edtech-horizon
    env: docker
    dockerfilePath: Dockerfile
    dockerCommand: php artisan horizon
    plan: free
    envVars:
      - key: APP_ENV
        value: production
      - key: APP_DEBUG
        value: false
      - key: APP_KEY
        fromService:
          type: web
          name: edtech-api
          property: envVar:APP_KEY
      - key: DB_CONNECTION
        value: pgsql
      - key: DB_URL
        fromDatabase:
          name: edtech-db
          property: connectionString
      - key: REDIS_CLIENT
        value: phpredis
      - key: REDIS_URL
        fromService:
          type: web
          name: edtech-api
          property: envVar:REDIS_URL
      - key: CACHE_STORE
        value: redis
      - key: QUEUE_CONNECTION
        value: redis
      - key: SESSION_DRIVER
        value: redis

databases:
  - name: edtech-db
    plan: free
